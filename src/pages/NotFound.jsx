import React from "react";
import NotFoundPage from "../components/common/NotFound";

const NotFound = () => {
  return (
    <div>
      <NotFoundPage />
    </div>
  );
};

export default NotFound;
